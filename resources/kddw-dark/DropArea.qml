/*
  DropArea for KDDockWidgets dark theme.
  Unchanged from upstream — included so FloatingWindow.qml's local import resolves.
*/

import QtQuick 2.9

DropArea {
    id: root
    property QtObject dropAreaCpp: null
}
