# Build directories
build/
.cache/
*.so
*.so.*
*.a
*.o
*.dylib
*.dll
*.exe

# Qt
*.pro.user
moc_*.cpp
ui_*.h
qrc_*.cpp
*.autosave
*.qm
moc_*

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.cmake
!CMakeLists.txt

# LSP and formatting
compile_commands.json

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Application files
layout.json
*.log
.qmlls.ini

# Source clone of maplibre-native-qt (used to produce the prebuilt below).
# Not committed — too large. Recreated on demand via `just bootstrap-maplibre`.
third_party/maplibre-native-qt/

# Whitelist the committed prebuilt QMapLibre artifacts. These are normally
# matched by *.cmake, *.dylib, *.a above; keep them tracked.
!third_party/maplibre-prebuilt/**
