{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "just build",
      "type": "shell",
      "command": "cmd",
      "args": [
        "/c",
        "just build"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": "$msCompile"
    }
  ]
}